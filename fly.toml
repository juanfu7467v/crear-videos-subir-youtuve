app = 'crear-videos-subir-youtuve'
primary_region = 'iad'

[http_service]
  internal_port = 8080
  force_https = true
  auto_start_machines = true
  auto_stop_machines = true  # Fly apagará la máquina si no hay actividad web
  min_machines_running = 0

[[vm]]
  memory = '1gb'
  cpus = 1
